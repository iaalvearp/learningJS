// Funcion para obtener la mediana de un Array
/*const mediana = (lista) => {
    if (lista.length % 2 == 0) {
      lista.sort();
      let mitad = lista.length / 2;
      let res = lista[mitad - 1];
      return res;
    } else {
      lista.sort();
      let mitad = lista.length / 2;
      let res = lista[mitad - 0.5];
      return res;
    };
  }; */