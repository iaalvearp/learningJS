// Funcion para obtener la cantidad de repeticiones de elementos en un array
/*const resultado = {};
numeros.map( elemento => {
  if (resultado[elemento]) {
    resultado[elemento] += 1;
  } else {
    resultado[elemento] = 1;
  };
}); */
